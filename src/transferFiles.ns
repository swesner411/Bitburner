const filesToTransfer = [
	'node.ns'
]
const start = "node.ns"

export async function main(ns) {
	ns.tprint("Transfering files");
	let servers = await ns.scan("home")
	for (let i = 0; i < servers.length; i++) {
		await ns.scp(filesToTransfer, ns.getHostname(), servers[i])
	}
}
