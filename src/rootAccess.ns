const servers = await ns.scan("home");

export async function main(ns) {
	print("Establishing Root Access");
	for(const i = 0; i < servers.length; i++) {
		if (hasRootAccess(servers[i]) == false) {
			nuke(servers[i]);
		}
	}
}
