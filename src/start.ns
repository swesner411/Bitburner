export async function main(ns) {
  if (ns.getHostname() !== "home") {
    throw new Exception("Run this script from home");
  }
  await ns.wget(
    `https://raw.githubusercontent.com/swesner411/bitburner/dev/src/downloadFiles.ns`,
    "downloadFiles.ns"
  );
  ns.run("downloadFiles.ns", 1);
}