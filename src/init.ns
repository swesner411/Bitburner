export async function main(ns) {
	let hostname = ns.getHostname()
	if (hostname !== 'home') {
		throw new Exception('Run this script from home')
	}
	await ns.run('downloadFiles.ns',1);
	await ns.run('rootAccess.ns', 1);
	await ns.run('transferFiles.ns', 1);
}
